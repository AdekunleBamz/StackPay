[project]
name = "stackpay"
description = ""
authors = []
telemetry = false
cache_dir = "./.cache"

[contracts.stackpay]
path = "contracts/stackpay.clar"
clarity_version = 2
epoch = 2.5

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = true
api_url = "https://api.hiro.so"
